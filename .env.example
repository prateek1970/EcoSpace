# EchoSpace Environment Configuration
# -----------------------------------
# Copy this file to .env and fill in values. NEVER commit .env to git.

# MongoDB Atlas Connection String (REQUIRED)
MONGODB_URI=mongodb+srv://Deep2004:Deep2004@prateek.qwa3k5w.mongodb.net/echospace?retryWrites=true&w=majority&appName=Prateek

# Server Port
PORT=3000

# Session Secret — MUST be a long, random string (min 32 chars)
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=echospace_secret_key_2026

# Node Environment: 'development' or 'production'
NODE_ENV=development
