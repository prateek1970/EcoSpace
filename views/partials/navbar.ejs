<nav class="navbar navbar-expand-lg navbar-dark sticky-top echo-navbar">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center gap-2 fw-bold text-warning" href="/">
      <div class="brand-icon-box bg-warning text-dark rounded-circle d-flex align-items-center justify-content-center" style="width: 38px; height: 38px;">
        <i class="bi bi-soundwave fs-4"></i>
      </div>
      <span class="fs-4 tracking-wide text-light">Echo<span class="text-warning">Hub</span></span>
    </a>
    
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#echoNavbar" aria-controls="echoNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="echoNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4 gap-1">
        <li class="nav-item">
          <a class="nav-link text-light px-3 rounded text-hover-warning" href="/">
            <i class="bi bi-disc me-1 text-warning"></i> Showcase
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-light px-3 rounded text-hover-warning" href="/#collab-section">
            <i class="bi bi-people me-1 text-info"></i> Collab Hub
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-light px-3 rounded text-hover-warning" href="/admin">
            <i class="bi bi-sliders me-1 text-danger"></i> Studio Admin
          </a>
        </li>
      </ul>
      
      <div class="d-flex align-items-center gap-2 mt-2 mt-lg-0">
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <!-- Logged In State -->
          <a href="/#collab-section" class="btn btn-glass btn-sm d-flex align-items-center gap-1 rounded-pill px-3 fw-semibold text-warning">
            <i class="bi bi-mic-fill"></i> Submit Collab Call
          </a>
          <a href="/auth/profile" class="btn btn-glass btn-sm d-flex align-items-center gap-1 rounded-pill px-3 fw-semibold text-light">
            <div class="d-flex align-items-center justify-content-center rounded-circle me-1" style="width: 22px; height: 22px; background: rgba(255,193,7,0.25); border: 1px solid rgba(255,193,7,0.5); font-size: 11px;">
              <span class="fw-bold text-warning"><%= currentUser.name.charAt(0).toUpperCase() %></span>
            </div>
            <%= currentUser.name.split(' ')[0] %>
          </a>
          <a href="/auth/logout" class="btn btn-glass btn-sm d-flex align-items-center gap-1 rounded-pill px-3 text-danger" title="Logout">
            <i class="bi bi-box-arrow-right"></i>
          </a>
        <% } else { %>
          <!-- Logged Out State -->
          <a href="/auth/login" class="btn btn-glass btn-sm d-flex align-items-center gap-1 rounded-pill px-3 fw-semibold text-light">
            <i class="bi bi-box-arrow-in-right"></i> Login
          </a>
          <a href="/auth/register" class="btn btn-warning btn-sm d-flex align-items-center gap-1 rounded-pill px-3 fw-bold text-dark shadow-sm">
            <i class="bi bi-person-plus-fill"></i> Register
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

